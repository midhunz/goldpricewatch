{
    # Global options
    email {$ACME_EMAIL}
}

{$DOMAIN_NAME} {
    # Backend API
    handle /api/* {
        reverse_proxy backend:8000
    }

    # Frontend Next.js
    handle /* {
        reverse_proxy frontend:3000
    }

    # Compression
    encode gzip
}
